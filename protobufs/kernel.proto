syntax = "proto3"
package JupyterGoAPI;

message Kernel {
    required string id = 1;
    required string name = 2;
    optional string last_activity = 3;
    optional int32 connections = 4;
    optional string execution_state = 5;
}